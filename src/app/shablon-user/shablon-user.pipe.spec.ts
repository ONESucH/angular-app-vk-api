import { ShablonUserPipe } from './shablon-user.pipe';

describe('ShablonUserPipe', () => {
  it('create an instance', () => {
    const pipe = new ShablonUserPipe();
    expect(pipe).toBeTruthy();
  });
});
